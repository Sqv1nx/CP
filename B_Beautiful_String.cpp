#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin>>n;
        char s[n];
        int i;
        for(i=0;i<n;i++)
        {
            cin>>s[i];
        }   
        int final[n]={0};
    }
    return 0;
}